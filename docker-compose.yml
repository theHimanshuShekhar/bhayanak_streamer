version: "3.9"
services:
  streamer:
    build: .
    restart: unless-stopped
    ports:
      - 3000:3000
    env_file:
      - .env

  # tunnel:
  #   container_name: cloudflared-tunnel
  #   image: cloudflare/cloudflared:latest
  #   restart: unless-stopped
  #   command: tunnel run
  #   env_file:
  #     - .env

networks:
  bhayanak_network:
    external: true